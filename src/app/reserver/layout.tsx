// Layout neutre pour le moteur de réservation public (sans sidebar / header dashboard)
export default function PublicBookingLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
